<div class="card">
    <div class="card-content">
        <h4><a class="title" href="/post/{{id}}">{{title}}</a></h4>
        <p class="subtitle">Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>

    <div class="content">{{content}}</div>
</div>